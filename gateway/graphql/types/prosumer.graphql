

type Query {
    get_prosumer(id:ID!):Prosumer
    weatherData: Weather
    
}
type Mutation {
    set_prosumer(id:ID!): Boolean!
    set_temp(tick: Int, data: Float): Weather
}
type Prosumer {
    id: ID!
    consumption: Float
}

type Weather {
    tick: Int
    # The tweet text. No more than 140 characters!
    data: Float
    # When the tweet was published
    
}