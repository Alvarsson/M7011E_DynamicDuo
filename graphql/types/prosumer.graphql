

type Query {
    prosumer(id:ID!):Prosumer
    weatherData: Weather
    
}
type Prosumer {
    id: ID!
    # The tweet text. No more than 140 characters!
    body: String
    # When the tweet was published
    
}
type Weather {
    temp: Int
    # The tweet text. No more than 140 characters!
    windspeed: Int
    # When the tweet was published
    
}